name: precipitation_radial_widget
description: Standalone Android widget showing radial precipitation forecast from PirateWeather API.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  shared_preferences: ^2.3.0
  home_widget: ^0.7.0
  geolocator: ^13.0.0
  flex_color_picker: ^3.6.0
  material_design_icons_flutter: ^7.0.7296
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
